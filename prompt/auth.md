when new user first enter the app, show them all template, and let them choose one, then create a default resume for them.  
- during resume editing, if user leave just let them know they will lost data after leaving page. instead of redirect them to dashboard. and suggest theme to create account but don't force them to create an account.
- if they leave the app just let them go to home page.
- when they click on export pdf button, as theme to create account for manage cv letter, but don't force them to create an account, let them download the cv without create account.
- for sharing cv link, must need to have account.
- if user already login, show the dashboard page for him/her.  and dashboard can able to logout too.

